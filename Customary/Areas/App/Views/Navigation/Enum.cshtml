@model Custom.Areas.App.Models.EnumViewModel
@{
    var viewport = new Custom.Presentation.Sencha.Ext.container.Viewport.Builder();
}
<script type="text/javascript">
    Ext.Define("App.metadata.Enum", function () {

        return @{ Model.Panel.ToSerializer().Serialize(Html.ViewContext.Writer); };
    });
</script>
