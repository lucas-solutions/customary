@model Custom.Presentation.Sencha.Ext.data.Store
@using Custom.Areas.Metadata.Models
@using Custom.Metadata
@using Custom.Presentation
@using Ext = Custom.Presentation.Sencha.Ext
<script type="text/javascript">
    Ext.define('App.metadata.type.Store', function () {
        return {
            extend: 'Ext.data.Store',
            requires: [
                'App.metadata.type.Model',
                'App.metadata.type.Proxy'
            ],
            initComponent: function (config) {

                Ext.apply(this, {
                    model: Ext.create('App.metadata.type.Model', {}),
                    proxy: Ext.create('App.metadata.type.Proxy', {})
                });

                this.callParent();
            }
        };
    });
</script>
